# Spectral Service Integration Requirements
# Install with: pip install -r requirements_spectral.txt

# Core FastAPI dependencies
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9  # Required for file uploads

# HTTP client for agent
requests>=2.32.0

# ML/Scientific Computing (already in your project)
torch>=2.0.0
numpy>=1.24.0
scipy>=1.10.0

# Data loading
# astropy>=5.3.0  # Optional, for FITS file support

# Model training (already in your project)
optuna>=3.0.0
mlflow>=2.10.0

# Pydantic for schemas
pydantic>=2.0.0
